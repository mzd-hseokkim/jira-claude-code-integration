# Approach: MAE-459 - [TEST] v0.59.0 jira-cli 검증 — run-log README 스키마 표의 quarantined/passed 타입 수정

- **Level**: L1 (Single)
- **Source**: N/A — discover 생략, Jira 이슈 설명이 출처

## Approach (L1 Single)

- **변경 영역**: `docs/run-log/README.md` 스키마 표의 `quarantined`·`passed` 두 행만 수정.
- **핵심 결정**: 표 타입을 `boolean | null` 대신 `array | null`로 고치고 요소 구조(`{taskId, deferredKind}` / `taskId`)를 설명란에 명시 — `append-run-log.py`의 `build_entry()`가 `result` 값을 그대로 통과시키므로 실제 `_index.jsonl`의 loop-run 라인이 정본이다.
- **검증**: README 표 두 행이 배열 타입으로 기술되는지 육안 확인 + `npm test` 통과.
- **리스크**: 없음 — 문서 전용 변경이며 스크립트·스키마 코드 경로에 영향 없음.
- **롤백**: 단일 파일 2행 변경이므로 `git revert` 한 번으로 원복.
