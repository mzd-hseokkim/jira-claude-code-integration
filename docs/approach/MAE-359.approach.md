# Approach: MAE-359 - approach 통합 이후 남은 stale reference 정리 (start PDCA 권고, auto 출력 예시)

- **Level**: L1 (Single)
- **Source**: N/A — Jira 이슈 설명 (변경 위치·라인 명시됨)

## Approach (L1 Single)

- **변경 영역**: `skills/jira-task-start/SKILL.md` Step 7 + `skills/jira-task-auto/SKILL.md` Step 1.5 + `.claude-plugin/plugin.json`
- **핵심 결정**: 단계명 `plan`/`design` → 현행 단계명 `approach`로 직접 치환 (Edit, surgical), 새 추상화/리팩토링 없음
- **검증**: 두 파일에 grep `plan, design`/`design 스킵`/`design 단계명 잔재` 0건 + plugin.json `version` bump 확인
- **리스크**: 없음 (마크다운 텍스트 변경, 동작 영향 없음)
- **롤백**: 단일 커밋 revert로 즉시 복구 가능
