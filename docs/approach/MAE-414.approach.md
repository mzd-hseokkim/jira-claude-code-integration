# Approach: MAE-414 - auto 체이닝에 사용자 단계 스킵 제어

- **Level**: L1 (Single)
- **Source**: N/A — discover 생략 (Jira 이슈 description 기반)

## Approach (L1 Single)

- **변경 영역**: `skills/jira-task-auto/SKILL.md` 단일 파일 — Step 1.5 인근에 사용자 명시 스킵 처리 규칙 추가 (`.claude-plugin/plugin.json` version bump 동반).
- **핵심 결정**: 새 플래그/스키마 추가 대신 자연어 스킵 인입 — Step 1.5의 "사용자가 자연어로 권고를 뒤집으면 따른다" 패턴을 양방향으로 확장. 기존 PDCA 권고 로직은 상위 레이어로 보존하고, 사용자 명시 스킵을 그 위 우선순위로 둔다.
- **검증**: 스킵 지정 단계가 sub-agent 호출 없이 한 줄 사유만 출력하고 `completedSteps`에 추가되지 않으며, 나머지 단계는 정상 순차 실행됨을 프롬프트 문구로 확인.
- **리스크**: 스킵 단계가 후속 단계의 입력(예: approach 스킵 → impl 입력 부재)일 때 깨질 수 있음 — 의존 단계 스킵 시 사용자에게 영향 고지 문구로 완화.
- **롤백**: 단일 마크다운 파일 + version bump이므로 git revert로 즉시 원복 가능.
