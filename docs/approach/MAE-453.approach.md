# Approach: MAE-453 - [TEST] auto Workflow 검증 — README에 버전 배지 줄 추가

- **Level**: L1 (Single)
- **Source**: N/A — discover 생략 (Jira 이슈 설명이 유일한 출처)

## Approach (L1 Single)

- **변경 영역**: `README.md` 최상단 제목(`# jira-integration · Claude Code Plugin`) 바로 아래 한 줄만 추가, 다른 파일은 손대지 않는다.
- **핵심 결정**: 기존 shields.io 배지 블록을 고치는 대신 blockquote 한 줄(`> Current version: 0.52.0`)을 별도로 추가한다 — 이슈가 지정한 형식이고, 배지(0.34.0)는 이번 범위 밖이라 stale 값을 건드리면 변경이 요구사항을 넘어선다.
- **검증**: `grep -n "Current version: 0.52.0" README.md`로 줄 존재를 확인하고 `npm test`가 통과하는지 본다.
- **리스크**: 낮음 — 문서 한 줄 추가라 코드 영향 없음. 다만 기존 Version 배지(0.34.0)와 값이 달라 문서상 버전 표기가 두 곳으로 갈린다.
- **롤백**: 해당 커밋 `git revert` 한 번으로 원복 (단일 파일·단일 라인).
