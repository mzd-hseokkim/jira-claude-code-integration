# Approach: MAE-458 - [TEST] v0.58.0 호출 수 절감 검증 — run-log README에 loop-run 라인 예시 추가

- **Level**: L1 (Single)
- **Source**: N/A — discover 생략, Jira 이슈 설명이 출처

## Approach (L1 Single)

- **변경 영역**: `docs/run-log/README.md`의 `### 예시` 절 한 곳 (json 블록에 `loop-run` 라인 1줄 추가), 다른 파일 변경 없음.
- **핵심 결정**: 요약·가공한 샘플 대신 `_index.jsonl`에 실제로 기록된 loop-run 라인(passed 2건 `MAE-455`/`MAE-457`, quarantined 1건 `MAE-456`)을 그대로 인용 — 스키마 문서가 실제 기록과 어긋나는 것을 막기 위함.
- **검증**: `docs/run-log/README.md`에 `"kind": "loop-run"` 문자열이 존재하고, `npm test`가 통과한다.
- **리스크**: 인용할 실제 라인의 `quarantined`/`passed`가 배열인데 README 스키마 표는 `boolean | null`로 적혀 있어 예시와 표가 불일치해 보인다 — 본 티켓 범위는 예시 추가뿐이므로 표 수정은 하지 않고 관찰 사항으로만 남긴다.
- **롤백**: 문서 한 줄 추가라 해당 커밋을 `git revert` 하면 완전 복구된다.
