# Approach: MAE-413 — Phase gate를 레벨 기반 advisory로 (L1 한정)

> **Level**: L1 (Single)
> **Status**: Draft
> Generated by `/jira-task approach`

## Approach (L1)

- **변경 영역**: `hooks/scripts/phase-gate.js`, `hooks/scripts/phase-gate.config.json`, `.claude-plugin/plugin.json`(version bump).
- **핵심 결정**: hard-block을 기본으로 유지하고, 작업 level이 L1일 때만 gate 위반을 차단 대신 advisory warning(경고 후 진행 허용)으로 강등. level 소스는 `.jira-context.json`의 `breakdownLevel`, 우회는 `bypassGate`와 연동.
- **검증**: L1 task에서 gate 위반 시 경고만 뜨고 진행되는지, L2/L3는 기존대로 hard-block 되는지, `bypassGate` 동작 유지되는지 확인.
- **리스크**: `breakdownLevel`이 미설정/누락된 task는 보수적으로 hard-block 유지(L1로 오인 강등 금지). 선행 spine 작업의 level 소스와 약결합.
- **롤백**: 두 파일 변경 revert + version 원복. config 토글로 advisory 비활성화 가능하면 토글 우선.
