# Approach: MAE-455 - [TEST-1] run-log 스키마 문서 docs/run-log/README.md 추가

- **Level**: L1 (Single)
- **Source**: N/A — discover 생략, Jira 이슈 description 기준

## Approach (L1 Single)

- **변경 영역**: `docs/run-log/README.md` 신규 1개 파일만 추가 (기존 `_index.jsonl`과 같은 디렉터리).
- **핵심 결정**: 필드 설명을 임의로 재서술하는 대신 `scripts/append-run-log.py`의 `build_entry()` 반환 키를 정본으로 그대로 옮긴다 — 문서와 코드가 갈라지는 것을 막기 위해.
- **검증**: `build_entry()`의 모든 키(`taskId`, `timestamp`, `kind`, `status`, `failedStage`, `reason`, `stagesRun`, `skipped`, `fixAttempts`, `innerLoopIterations`, `breakdownLevel`, `metrics`, `stageDurationsSec`, `harnessVersion`, `quarantined`, `passed`)가 표에 있는지 대조하고 `npm test` 통과를 확인한다.
- **리스크**: 없음 — 문서 신규 추가라 실행 경로에 영향이 없다.
- **롤백**: 파일 삭제 또는 커밋 revert로 완전 복구 가능.
