# Approach: MAE-361 - [Bug] jira-task-impl 스킬이 stale한 docs/design·docs/plan 경로를 참조 (approach 통합 누락)

- **Level**: L1 (Single)
- **Source**: N/A — discover 생략 (Jira description의 위치 표 + Consolidated AC 코멘트가 입력)

## Approach (L1 Single)

- **변경 영역**: `skills/jira-task-impl/SKILL.md` 단일 파일 (Prerequisites/Step 1/Step 2 본문) + `.claude-plugin/plugin.json` version bump.
- **핵심 결정**: `docs/design/...`/`docs/plan/...` 참조를 `docs/approach/<TASK-ID>.approach.md` 단일 경로로 일괄 치환 — fallback 분기 제거 (마이그레이션 alias가 approach 디렉토리를 보장하므로 분기 불필요).
- **검증**: 변경 후 SKILL.md grep `docs/design`/`docs/plan` 0건, `docs/approach` 참조가 Prerequisites·Step 1·Step 2에 존재.
- **리스크**: 없음 — 텍스트 경로 교체만, 실행 동작 변경 없음.
- **롤백**: 단일 파일 revert로 완전 복구 가능.
