# Test Report: MAE-169

**Date**: 2026-04-27
**Branch**: feature/MAE-169
**Topic**: [1.1.10] discover 합성 결과에 Trace Marker 자동 부여

## Summary

| Metric | Value |
|--------|-------|
| Total Tests | 10 |
| Passed | 10 |
| Failed | 0 |
| Skipped | 0 |
| Duration | ≈ 1분 (grep/diff 기반) |
| Result | PASS |

## Test Strategy Note

본 이슈는 코드 산출물이 아닌 SKILL.md(LLM 가이드) 변경이라 자동 unit/E2E 테스트가 적용되지 않는다. Design 문서 Test Plan에 따라 다음 두 가지 방식을 채택했다.

- **Document inspection**: SKILL.md/plugin.json/Inline Fallback Template diff에 대한 grep/시각 검증
- **Dry-run sanity check**: Inline Fallback Template이 4 marker 형식을 모두 예시로 보여주는지 mental walk-through

프로젝트에 `package.json`/`playwright.config.*`/`vitest.config.*`/`pytest.ini` 등 테스트 프레임워크 설정이 없음을 확인했다.

## Unit Tests

해당 없음 (코드 변경 없음).

## E2E Tests (Playwright)

해당 없음 (실행 가능한 UI/서비스 없음).

## Document Inspection Results

### TC-1: SKILL.md Step 2에 코드 메타 보존 형식 명시 — PASS (AC-1)

- **검증**: `grep "file_path.*line_range" skills/jira-task-discover/SKILL.md`
- **매칭**: L92 ("**결과 메타 보존 형식**: 각 발췌는 `(file_path, line_range)` 튜플 형태로 보존한다"), L93 (line_range 표기 예시 `<path>:<start>-<end>`), L95 (민감 파일 제외 가이드)
- **결과**: 3건 매칭. AC-1 충족.

### TC-2: SKILL.md Step 3에 Q1~Q4 인덱스 규칙 명시 — PASS (AC-2)

- **검증**: Step 3 영역(L99-121)에서 `Q1`/`Q2`/`Q3`/`Q4` 토큰 검색
- **매칭**: L107-110 (표에 Q1~Q4 인덱스 부여), L112-115 (인덱스 부여 규칙 본문 + lite/from 모드 처리), L117 (lite 모드 Q1~Q3)
- **결과**: 4건 모두 매칭 + lite 모드 명시. AC-2 충족.

### TC-3: SKILL.md Step 4에 marker 형식 4종 모두 명시 — PASS (AC-3)

- **검증**: Step 4 영역(L147-198)에서 4 marker 패턴 grep
- **매칭** (L168-173 표 형태):
  1. 답변 1개 → `*(source: Q<N>)*`
  2. 코드 1곳 → `*(code: <path>:<line-range>)*`
  3. 답변+코드 결합 → `*(source: Q<N>, code: <path>:<line>)*`
  4. LLM 합성 → `*(synthesized)*`
  5. (보너스) 다중 출처 → `*(source: Q1, Q3)*`, `*(code: src/a.ts:10-20, src/b.ts:5-15)*`
- **결과**: 4 패턴 모두 한 표에서 확인. AC-3 충족.

### TC-4: Inline Fallback Template placeholder에 marker 자리 추가 — PASS (AC-4)

- **검증**: L242-311 Inline Fallback Template에서 4섹션 placeholder 끝 marker 자리 확인
- **매칭**:
  - `## Functional Requirements` (L274-281): 4 marker 형식 모두 등장 (source / source+code / code / synthesized)
  - `## Edge Cases` (L283-288): synthesized + code
  - `## Out of Scope` (L290-295): source + synthesized
  - `## Open Questions` (L297-301): source: Q<N>
- **결과**: 4 섹션 모두 marker 자리 포함. 모든 marker 형식 4가지가 placeholder에 적어도 한 번씩 등장. AC-4 충족.

### TC-5: SKILL.md에 synthesized 가이드 명시 — PASS (AC-5)

- **검증**: Step 4 영역 `synthesized` 가이드 섹션 검색
- **매칭** (L189-194):
  - L189: 헤더 ("**`*(synthesized)*` 사용 가이드 (남용 방지):**")
  - L191: 사용 가능 조건 ("Q<N> 답변 어디에도 직접 근거가 없고, Step 2 코드 발췌 어디에도 직접 근거가 없는 LLM 자체 합성 항목에만 사용")
  - L192: 권장 우선순위 (`Q<N>` > `code:` > 둘 다 결합 > `synthesized`)
  - L193: Open Questions 예외
  - L194: Edge Cases 기본 marker 가이드
- **결과**: 4 bullet 모두 명시. AC-5 충족.

### TC-6: --lite 모드 정합성 명시 — PASS (AC-6)

- **검증**: --lite 모드 본문에 marker 적용 대상 자연 축소 노트 검색
- **매칭** (L196-198): "**`--lite` 모드 정합성:**" 헤더 + "Edge Cases/Out of Scope 섹션이 통째로 생략되므로 marker 적용 대상은 자연 축소되어 **Functional Requirements + Open Questions 2개 섹션**만 남는다. Q 인덱스 범위도 `Q1`~`Q3`로 축소"
- **결과**: 1건 매칭. AC-6 충족.

### TC-7: Surgical change — PASS (AC-7)

- **검증**: `git diff --stat`로 변경 파일 목록 확인
- **변경 파일**:
  - `.claude-plugin/plugin.json` (2 lines)
  - `skills/jira-task-discover/SKILL.md` (87+/23-)
- **untracked (산출 문서)**: `docs/plan/MAE-169.plan.md`, `docs/design/MAE-169.design.md` (PDCA 산출물, 변경 대상 외)
- **결과**: 다른 jira-task-* 스킬, `commands/jira-task.md`, `templates/*` 모두 미터치. AC-7 충족.

### TC-8: plugin.json version 증가 — PASS (AC-8)

- **검증**: `.claude-plugin/plugin.json`의 `version` 필드 비교
- **결과**: `0.15.1` → `0.15.2` (semver patch +1). AC-8 충족.

### TC-9: Dry-run sanity check — PASS

- **검증**: 가상 topic("사용자 알림 시스템")으로 워크플로 mental walk-through
- **결과**:
  - Step 2: `(file_path, line_range)` 메타 보존 가이드 명시되어 LLM이 `src/notify.ts:45-60` 형식으로 보존 가능
  - Step 3: `Q1`~`Q4` 인덱스 자동 부여 가이드 명시
  - Step 4: 합성 4종(FR/Edge/OOS/OQ) 항목마다 marker 부여 가이드 명시
  - Inline Fallback Template L278-281이 4 marker 형식 모두 예시로 보여주어 LLM 모방 가능
- **비고**: LLM 추론은 비결정적이므로 1회 sanity check로 충분. CI 자동 검증은 후속 이슈.

### TC-10: Jira 첨부 marker 노출 검토 (Security) — PASS

- **검증**: SKILL.md Step 2에 민감 파일 제외 가이드 존재 여부
- **매칭** (L95): "**민감 파일 제외**: `.env*`, `.claude/settings.local.json`, `node_modules/`, 자격증명·토큰을 포함한 파일은 발췌·메타에서 제외한다 (산출 문서가 Jira 첨부로 전송될 수 있음)"
- **결과**: 보안 가이드 1건 명시. Design Security Checklist 항목 충족.

## Failed Tests Detail

없음.

## Verification against Acceptance Criteria

| AC | 충족 여부 | 검증 TC |
|----|---------|---------|
| AC-1 (Step 2 메타 보존) | PASS | TC-1 |
| AC-2 (Q<N> 인덱스) | PASS | TC-2 |
| AC-3 (marker 자동 부여) | PASS | TC-3 |
| AC-4 (Template placeholder) | PASS | TC-4 |
| AC-5 (synthesized 가이드) | PASS | TC-5 |
| AC-6 (--lite 정합성) | PASS | TC-6 |
| AC-7 (Surgical) | PASS | TC-7 |
| AC-8 (plugin.json version) | PASS | TC-8 |

## Screenshots

해당 없음 (Playwright 미적용).

## Notes

- 본 이슈는 LLM 가이드(SKILL.md) 변경이므로 marker 형식 일관성의 최종 검증은 실제 discover 호출 시 발생한다. 회귀가 발생하면 SKILL.md L168-173 표를 SSOT로 사용해 수정한다.
- MAE-171 (templates/requirements.template.md 갱신)이 본 이슈의 marker 형식을 그대로 복제해야 한다. 본 이슈가 SSOT.
