# Test Report: MAE-171

**Date**: 2026-04-27
**Branch**: feature/MAE-171

## Summary

| Metric | Value |
|--------|-------|
| Total Tests | 9 (정적/수동) |
| Passed | 9 |
| Failed | 0 |
| Skipped | 0 |
| Duration | < 1s (정적 검증) |
| Result | **PASS** |

## Test Type

런타임 코드 변경이 없는 문서 동기화 작업이므로 자동화 테스트 프레임워크는 사용하지 않는다. Design 문서의 Test Plan에 명세된 9개 정적/수동 검증 케이스(TC-1~TC-9)를 `grep` / `diff` / `git diff` 등으로 직접 실행.

## Test Cases

### TC-1: Goals 측정 sub-template 적용 — PASS (AC-1)
- 명령: `grep -A 6 "## Goals" templates/requirements.template.md`
- 결과: `<지표명> · <현재값> → <목표값> · <측정방법>` 형식 안내문 + 2건 예시 (응답 시간, 합성 정확도) 등장.

### TC-2: FR trace 필드 적용 — PASS (AC-2)
- 명령: `grep -A 6 "## Functional Requirements" templates/requirements.template.md`
- 결과: 4종 marker 케이스 모두 등장:
  - `*(source: Q2, code: src/notify.ts:45-60)*` (Q+code 결합)
  - `*(source: Q1)*` (Q-only)
  - `*(code: src/foo.ts:12-30)*` (code-only)
  - `*(synthesized)*` (LLM 합성)

### TC-3: trace marker 형식 1.1.10과 일치 — PASS (AC-2, 1.1.10 호환)
- TC-2 결과를 SKILL.md L237-242 marker 표준 표와 대조.
- 형식·콤마 구분·구두점 모두 일치.

### TC-4: Open Questions P1/P2/P3 마커 — PASS (AC-3)
- 명령: `grep -A 8 "## Open Questions" templates/requirements.template.md`
- 결과: 안내문에 `P1: 다음 단계 차단 / P2: 확인 필요 / P3: 참고` 의미 명시. 예시 `[P1]/[P2]/[P3]` 3건 + `[CONFLICT]` 1건 등장. `[CONFLICT]` 항목은 우선순위 마커와 trace marker 모두 부착하지 않음 (안내문 명시).

### TC-5: Inline Fallback 동기화 — PASS (AC-4)
- TC-6의 `diff` exit 0이 자동으로 입증.

### TC-6: byte-level 1:1 매칭 (핵심 게이트) — PASS (AC-5)
- Inline Fallback 라인 범위: **394..464**
- 명령: `diff <(sed -n '394,464p' skills/jira-task-discover/SKILL.md) templates/requirements.template.md`
- 결과: **출력 없음, exit 0** — byte-level 1:1 매칭 복원 확인. MAE-117 AC-2 회복.

### TC-7: --lite 안내 주석 유지 — PASS (AC-6, MAE-117 AC-4)
- 명령: `grep -- "--lite 모드" templates/requirements.template.md | wc -l`
- 결과: 3건 (NFR placeholder, Edge Cases 주석, Out of Scope 주석). 모두 보존됨.

### TC-8: H2 섹션 구조 호환 — PASS (AC-6, MAE-117 AC-2/AC-3)
- 명령: 양 파일 H2 헤더 추출 후 `diff`
- 결과: H2 10개 (Stakeholders, Goals & Success Criteria, Constraints, Non-functional Requirements, Codebase Context, Functional Requirements, Edge Cases, Out of Scope, Open Questions, Proposed Issue Breakdown). diff exit 0.

### TC-9: surgical change 원칙 확인 (--from 분기 호환) — PASS (AC-6)
- 명령: `git diff skills/jira-task-discover/SKILL.md | grep "^@@"`
- 결과: 변경 hunk 2개, 모두 L404~L457 범위 (= Inline Fallback 코드 블록 내부). SKILL.md 본문(L245-254 `--from` marker 변형 표 등)은 손대지 않음.

## Failed Tests Detail

없음 — 9건 모두 통과.

## Acceptance Criteria 충족 매핑

| AC | 검증 케이스 | 결과 |
|----|-----------|------|
| AC-1 (Goals 측정 sub-template) | TC-1 | PASS |
| AC-2 (FR trace 필드 + 1.1.10 일치) | TC-2, TC-3 | PASS |
| AC-3 (Open Questions P1-P3) | TC-4 | PASS |
| AC-4 (Inline Fallback 동기화) | TC-5 (= TC-6) | PASS |
| AC-5 (byte-level 1:1 매칭) | TC-6 | PASS |
| AC-6 (--lite / 섹션 / --from 호환) | TC-7, TC-8, TC-9 | PASS |

## Conclusion

모든 AC가 자동/정적 검증으로 충족됨. 핵심 게이트인 TC-6 (`diff` exit 0)가 통과하여 MAE-117의 byte-level 매칭 규약(AC-2)이 회복됨.

## Re-run after Review Fix (W1)

리뷰 W1 (plugin.json version 미증가) 수정 후 재실행:
- TC-6 재실행: `diff` exit 0 (변경 없음, 매칭 유지)
- `.claude-plugin/plugin.json`: `0.17.5` → `0.17.6` 으로 변경됨 (CLAUDE.md 규약 충족)
- 다른 8개 TC는 placeholder/SKILL.md 본문 미변경이므로 재실행 불필요
