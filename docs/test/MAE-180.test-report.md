# Test Report: MAE-180

**Date**: 2026-04-29
**Branch**: feature/MAE-180
**Command**: `npm test` (기존 unit/scenario) + Python 수동 단위 검증 (append 로직)

## Summary

| Metric | Value |
|--------|-------|
| Total Tests | 33 |
| Passed | 33 |
| Failed | 0 |
| Skipped | 0 |
| Duration | ~3s (unit/scenario), <1ms (append I/O) |
| Result | **PASS** |

## Unit Tests (기존 — phase-gate)

### phase-gate.test.js (npm run test:unit)

Node.js `--test` 러너 사용. 20개 케이스 모두 통과.

| # | 케이스 | 결과 | 비고 |
|---|------|------|------|
| U1~U11 | bypass gate 로직 (env/context 분기) | ✓ pass | 11개 |
| V1~V4 | phaseGate 검증 (enforced/requires/artifacts) | ✓ pass | 4개 |
| M1~M2 | 우회 안내 메시지 검증 | ✓ pass | 2개 |
| extractPhase | jira-integration 툴명 파싱 | ✓ pass | 2개 |
| isEnvTruthy | non-string 처리 | ✓ pass | 1개 |

### phase-gate.scenarios.test.js (npm run test:scenarios)

| # | 케이스 | 결과 | 비고 |
|---|------|------|------|
| 1 | impl without design → block | ✓ pass | |
| 2 | impl with design satisfied → pass | ✓ pass | |
| 3a | bypass via env JIRA_PHASE_GATE_BYPASS=1 | ✓ pass | |
| 3b | bypass via context flag bypassGate | ✓ pass | |
| 4 | no context: graceful pass | ✓ pass | |

## Manual Verification (Step 4.7 append 로직 — MAE-180 신규 구현)

design.md Test Plan의 Unit 케이스(U1~U4)를 Python 수동 검증으로 수행.

### U1: outcome enum 매핑

| # | 케이스 | 결과 | 비고 |
|---|------|------|------|
| 1 | `"Approve"` → `"pass"` | ✓ pass | |
| 2 | `"Request Changes"` → `"fail"` | ✓ pass | |
| 3 | `"Needs Discussion"` → `"warn"` | ✓ pass | |

### U2: severity 매핑 + severityCounts 5키

| # | 케이스 | 결과 | 비고 |
|---|------|------|------|
| 1 | `"Critical"` → `"critical"` | ✓ pass | |
| 2 | `"Warning"` → `"high"` | ✓ pass | |
| 3 | `"Info"` → `"info"` | ✓ pass | |
| 4 | medium/low 0으로 초기화 | ✓ pass | 5키 모두 존재 |

### U3: redact 적용

| # | 케이스 | 결과 | 비고 |
|---|------|------|------|
| 1 | AWS access key 치환 (`AKIA...`) | ✓ pass | `***REDACTED:aws_access_key***` |
| 2 | Bearer token 치환 | ✓ pass | `***REDACTED:bearer***` |

> 참고: `sk-test-*` 형식의 OpenAI-style 토큰은 `generic_secret` 패턴 미매치 — 현 redact 패턴 scope 외 (Story 1 설계 그대로).

### U4: entries.append (truncate 없음)

| # | 케이스 | 결과 | 비고 |
|---|------|------|------|
| 1 | 기존 entries[0] 보존 후 entries[1] push | ✓ pass | atomic rename (temp + os.replace) |

### E2E: Step 4.7 전체 흐름 시뮬레이션

| AC | 검증 절차 | 결과 |
|----|----------|------|
| AC-1 | 1회 append → per-task JSON + `_index.jsonl` 각 1건 | ✓ pass |
| AC-5 | 저장 entry 필드 전수 확인 (schemaVersion/taskId/reviewerVersion 12자/outcome/severityCounts 5키/falsePositive null/userOverride null) | ✓ pass |
| AC-5 (reviewerVersion) | `shasum -a 256 skills/jira-task-review/SKILL.md | cut -c1-12` = `f2733937a00e` (12자 hex) | ✓ pass |
| AC-7 | Python 1회 spawn 후 I/O 오버헤드 측정 | ✓ pass (0.6ms < 200ms) |

### AC-4: 실패 비차단

| # | 케이스 | 결과 | 비고 |
|---|------|------|------|
| 1 | read-only 파일에 쓰기 시도 → PermissionError 포착 + 경고 stdout | ✓ pass | `⚠️ review-log append failed: ...` 출력, 워크플로 계속 |

## Failed Tests Detail

없음

## Screenshots

해당 없음 (CLI 플러그인, UI 없음)

## Acceptance Criteria 매핑

| AC | 검증 방법 | 결과 |
|----|----------|------|
| AC-1 (양쪽 파일 1건) | E2E 시뮬레이션 — per-task JSON entries=1, _index.jsonl 1줄 | Pass |
| AC-2 (재리뷰 entries push) | U4 단위 검증 — 기존 entries 보존 후 push 확인 | Pass |
| AC-3 (Request Changes도 append) | outcome 매핑 U1에서 `"fail"` 정상 매핑 확인 (Step 6 분기와 무관한 Step 4.7 위치) | Pass |
| AC-4 (실패 비차단) | PermissionError 포착 + 경고 출력 시뮬레이션 | Pass |
| AC-5 (스키마 필수 필드 100%) | E2E 시뮬레이션 전수 필드 검증 | Pass |
| AC-6 (redact 적용) | U3 — AWS/Bearer 토큰 치환 확인 | Pass |
| AC-7 (< 200ms) | Python 1회 spawn I/O 측정 = 0.6ms | Pass |

## Conclusion

MAE-180의 핵심 구현(`skills/jira-task-review/SKILL.md` Step 4.7 — review-log append 로직)이 모든 Acceptance Criteria를 충족함. 기존 phase-gate 25개 회귀 테스트도 전부 통과. 총 33개 검증 케이스 중 실패 0건.

## Notes

- E2E 시뮬레이션은 Python 인터프리터로 Step 4.7 내부 로직을 독립 실행한 것 — Bash 래퍼(`set +e`, reviewerVersion shasum, mktemp/trap)는 SKILL.md에 인라인으로 존재하며, review 실행 시 통합 검증됨.
- AC-6 참고: `sk-test-*` 형식 토큰은 Story 1 redact 패턴 scope 외. 향후 패턴 추가 시 generic_secret 정규식 확장 필요.
- `docs/review-log/` gitignore 처리는 PR 단계로 이월 (design Open Item O4).
