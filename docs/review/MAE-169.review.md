# Code Review: MAE-169

**Date**: 2026-04-27
**Branch**: feature/MAE-169
**Base**: main
**Topic**: [1.1.10] discover 합성 결과에 Trace Marker 자동 부여

## Summary

| 항목 | 값 |
|------|----|
| 결과 | Approve |
| 변경 파일 | 2개 (`.claude-plugin/plugin.json`, `skills/jira-task-discover/SKILL.md`) |
| 커밋 수 | 0 (작업 디렉터리 미커밋 상태 — impl 단계가 stage만 하지 않음) |
| 설계-구현 매칭률 | 100% (5/5) |
| Critical 이슈 | 0 |
| Warning 이슈 | 1 |
| Info 이슈 | 2 |

본 이슈는 SKILL.md(LLM 가이드) 텍스트 변경이며, 실행 코드 변경이 없다. Design 문서의 Implementation Plan 5개 행이 모두 충실히 반영되었고, AC 8개 모두 충족한다. 다만 변경분이 아직 git commit되지 않은 상태이므로 후속 단계(merge/PR)에서 커밋이 필요하다.

## Gap Analysis

Design 문서(`docs/design/MAE-169.design.md`)의 Implementation Plan File Change Summary 5개 행을 체크리스트로 변환하여 실제 변경과 직접 비교했다.

| # | Design 항목 | 구현 여부 | 위치 | 비고 |
|---|------------|----------|------|------|
| 1 | Step 2 본문에 `(file_path, line_range)` 메타 보존 형식 명시 | O | SKILL.md L92-95 | line_range 표기 예시 + 민감 파일 제외 가이드 포함 |
| 2 | Step 3 본문에 `Q1`~`Q4` (lite 시 `Q1`~`Q3`) 인덱스 부여 규칙 | O | SKILL.md L105-121 | 표 컬럼 추가 + 인덱스 부여 규칙 + lite/from 모드 처리 |
| 3 | Step 4 본문에 marker 형식 표 + `synthesized` 가이드 + `--from` 변형 | O | SKILL.md L147-198 | 5 case 표 + from 변형 4 case 표 + synthesized 4 bullet + lite 정합성 |
| 4 | Inline Fallback Template placeholder에 marker 자리 추가 | O | SKILL.md L274-301 | FR/Edge/OOS/OQ 4섹션 모두 marker 자리. 4 marker 형식이 placeholder에 적어도 한 번씩 등장 |
| 5 | `.claude-plugin/plugin.json` version 0.15.1 → 0.15.2 | O | plugin.json L3 | semver patch +1 |

**매칭률**: 5/5 = **100%**.

추가로 Design Decision 절(다중 출처 표기, `--from` 변형, `synthesized` 가이드)이 SKILL.md Step 4 본문에 그대로 옮겨졌는지 교차 점검했고 모두 일치했다.

## Lint & Format

프로젝트 루트에 `package.json`/`pyproject.toml`/`pom.xml`/`build.gradle*`이 모두 부재하며, 변경 파일도 마크다운(`.md`) + JSON(`plugin.json`) 2종이다. 따라서 ESLint/Prettier/ruff/checkstyle 모두 적용 대상이 아니다.

| 도구 | 대상 파일 수 | 결과 | 비고 |
|------|------------|------|------|
| ESLint | 0 | Skip | Node.js 프로젝트 아님 |
| Prettier | 0 | Skip | Node.js 프로젝트 아님 |
| ruff | 0 | Skip | Python 프로젝트 아님 |
| checkstyle | 0 | Skip | Java/Kotlin 프로젝트 아님 |

수동 점검 결과:
- `plugin.json`: 유효한 JSON. semver `0.15.2` 형식 정상.
- `SKILL.md`: 헤딩 레벨 일관 (####까지). 코드 블록 펜스 페어링 정상. 표 컬럼 정렬 정상.

## Code Quality Findings

### Critical
없음.

### Warning

- **변경분이 git commit되지 않음**: `git status` 기준으로 `.claude-plugin/plugin.json`, `skills/jira-task-discover/SKILL.md`가 working tree에만 존재. `git log --oneline main..feature/MAE-169` 결과가 비어 있음. impl 단계가 commit까지 만들지 않은 워크플로 특성으로 이해되지만, `/jira-local-merge`에서 수동 commit이 선행되어야 한다 (또는 merge 스킬이 자동 stage+commit한다면 그 흐름을 검증 필요).

### Info

- **표를 펜스 코드 블록으로 감싼 선택**: SKILL.md L165-174, L178-185 두 표가 ` ``` ` 코드 펜스로 감싸져 있다. Design 문서에서는 일반 마크다운 표로 두는 것도 가능했으나, 현 구현은 LLM이 표 자체를 한 덩어리로 인식하기 쉽고 marker 형식 예시(`*(source: Q1, Q3)*` 등)가 마크다운 이탤릭으로 렌더되는 부작용을 막아주므로 합리적이다. 다만 마크다운 미리보기에서 헤더 정렬이 빠진 채 monospace로 보이는 점은 명시적인 트레이드오프다.
- **Step 2의 "민감 파일 제외" 가이드는 design Security Checklist 부산물**: design 문서의 Security Checklist 첫 항목을 SKILL.md 본문에 직접 박았다. surgical 범위는 살짝 넘지만, 산출물이 Jira 첨부로 전송될 가능성을 고려하면 반영하는 편이 안전하므로 수용 가능. test report TC-10도 이 가이드를 검증하므로 일관성 OK.

## Positive Notes

- **Design SSOT 원칙 충실 반영**: Design 문서의 "Decision: 다중 출처 표기 규칙" / "Decision: `synthesized` 마커 사용 가이드" / "Decision: `--from` 모드의 marker 처리" 3개 결정이 SKILL.md Step 4 본문에 그대로 옮겨져 있다. 본 이슈가 marker 형식의 SSOT임을 design에서 명시했고 구현이 이를 정확히 따른다.
- **Inline Fallback Template의 학습용 분배**: `## Functional Requirements` placeholder L278-281에서 4 marker 형식(`source` only / `source+code` / `code` only / `synthesized`)이 모두 한 번씩 등장한다. LLM이 모방하기 쉬운 구조로, design AC-4 의도를 정확히 구현했다.
- **Surgical 변경 유지**: 변경 파일이 정확히 2개(`SKILL.md`, `plugin.json`)이며, 다른 jira-task-* 스킬, `commands/jira-task.md`, `templates/*` 모두 미터치. 동일 마일스톤(MAE-171)의 책임 영역을 침범하지 않음.
- **`--lite` 정합성 노트의 합리성**: `--lite` 모드에서 marker 적용 대상이 자연 축소(FR + Open Questions만)된다는 점을 명시적으로 박아두어, LLM이 lite 모드에서 불필요한 Edge/OOS marker를 만들지 않도록 가드가 됨.
- **plugin.json 버전 patch 선택**: SKILL.md 동작 가이드 변경은 minor 수준이 아니라는 판단으로 0.15.1 → 0.15.2 patch +1을 선택. CLAUDE.md 규칙 준수 + semver 의미상 적절.

---
Reviewed by Claude Code
