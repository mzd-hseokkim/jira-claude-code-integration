# Review: MAE-115 — [1.1.2] jira-task-discover 스킬 작성

- **결과**: Approve
- **검토 산출물**: `skills/jira-task-discover/SKILL.md` (1개, 13,239 bytes)
- **참조 문서**: `docs/plan/MAE-115.plan.md`, `docs/design/MAE-115.design.md`, `docs/test/MAE-115.test-report.md`
- **변경 형태**: 신규 마크다운 명세 파일 (untracked, 커밋 전)

## Summary

본 태스크의 산출물은 실행 코드가 아닌 SKILL.md 마크다운 명세 1개 파일이다. 따라서 핵심 검증은 **설계 문서와 구현된 명세의 매칭률**(Gap Analysis)이며, 코드 품질·린트·타입체크 항목은 적용 불가하다. 설계 문서 Implementation Plan의 14개 섹션 모두 SKILL.md에 빠짐없이 반영되어 있고, design test plan의 smoke check S1~S7 모두 통과하여 Approve로 판단한다.

## Gap Analysis

설계 문서 `Implementation Plan` 섹션의 14개 항목과 SKILL.md를 1:1 비교했다.

| # | Design 항목 | 구현 여부 | 위치 (SKILL.md) | 비고 |
|---|------------|----------|----------------|------|
| 1 | Frontmatter (name, description, argument-hint, allowed-tools, user-invocable) | O | L1–L13 | 5개 필드 모두 존재. allowed-tools에 Read, Write, Bash, Glob, Grep, AskUserQuestion 6종 모두 명시 |
| 2 | Language Rule (jira-task-plan과 동일 문구) | O | L17–L21 | jira-task-plan/SKILL.md의 Language Rule 본문과 diff 결과 일치 (S7 통과) |
| 3 | Overview (목적/입력/출력/Non-goals 요약) | O | L23–L39 | 입력 3종(자연어/--lite/--from), 출력 1종(`docs/requirements/<slug>.requirements.md`), Non-goals 4종 명시 |
| 4 | Input Model (인자 파싱 규칙 명세) | O | L41–L52 | 위치 인자/플래그 2종/조합 가능 규칙 모두 명세 |
| 5 | Workflow Step 0: Parse Arguments | O | L56–L68 | 토큰화, --lite/--from 추출, topic 누락 처리, --from 부재/1MB/빈 파일 분기 모두 포함 |
| 6 | Workflow Step 1: Slug Generation & Confirm | O | L70–L81 | LLM kebab 변환, `[a-z0-9-]` 강제, 60자 제한, 중복 시 `-2/-3` suffix, AskUserQuestion 1회 confirm |
| 7 | Workflow Step 2: Codebase Context Collection | O | L83–L93 | 키워드 3-5개 추출, Glob/Grep, 파일 10개·30줄 상한, 0건 시 디렉터리 트리 폴백, --from 통합 |
| 8 | Workflow Step 3: Batched Questions (4건/--lite 시 3건) | O | L95–L112 | AskUserQuestion 1회 호출 명시. 표로 4개 카테고리(이해관계자/성공기준/제약/비기능) 정리. --lite 시 4번 제외, --from 시 누락 항목만 선별 |
| 9 | Workflow Step 4: Generate Requirements Document | O | L114–L136 | 템플릿 우선순위(파일 존재 시 우선, 부재 시 인라인 fallback), 출력 경로 `docs/requirements/<slug>.requirements.md`, 섹션 목록 명시 |
| 10 | Workflow Step 5: Issue Breakdown Section | O | L138–L157 | 에픽 1 + 스토리 N + 서브태스크 M 트리. "이슈 만들지 않음 — 문서에만 기록" 명시 |
| 11 | Workflow Step 6: Completion Summary | O | L159–L178 | Progress 라인이 `discover ✓`만 표시, Next는 `/jira-task create --from-requirements` 안내. `.jira-context.json` 미수정 명시 |
| 12 | Inline Fallback Template (전문 인라인) | O | L180–L247 | 마크다운 구조 전문 포함. Stakeholders, Goals, Constraints, NFR, Codebase Context, Functional Reqs, Edge Cases, Out of Scope, Open Questions, Proposed Issue Breakdown 모두 포함 |
| 13 | Error Handling | O | L249–L265 | 13개 시나리오를 표로 정리. 자연어 누락/--from 부재/빈 파일/1MB 초과/슬러그 거부/슬러그 중복/키워드 0개/Glob 0건/모든 답변 빈 값/컨텍스트 폭주/템플릿 부재/--lite+--from 동시/위험 문자 모두 포함 (설계의 11개 + 추가 2개) |
| 14 | Non-goals | O | L267–L275 | Jira 이슈/`.jira-context.json`/`completedSteps`/라우팅/템플릿 생성/INDEX/외부 API 7종 명시 |

**매칭률: 14/14 = 100%**

추가로 design `Test Plan`의 smoke check를 모두 실행:

| 검증 | 기대 | 실측 |
|------|------|------|
| S1 frontmatter 5필드 | 5건 매치 | 5건 OK |
| S2 Language Rule 섹션 | 1건 매치 | 1건 OK |
| S3 Workflow Step 0~6 | 7건 매치 | 7건 OK |
| S4 Inline Fallback Template | 1건 매치 | 1건 OK |
| S5 Non-goals | 1건 매치 | 1건 OK |
| S6 `--lite`/`--from` ≥3건 | ≥3 / ≥3 | 15 / 16 |
| S7 Language Rule 본문 동일성 | 동일 | diff empty (동일) |

S1~S7 7건 모두 통과.

## Lint & Format

| 도구 | 대상 파일 수 | 결과 | 비고 |
|------|------------|------|------|
| ESLint | 0개 | N/A | JS/TS 변경 없음 |
| Prettier | 0개 | N/A | JS/TS 변경 없음 |
| ruff/flake8 | 0개 | N/A | Python 변경 없음 |
| checkstyle | 0개 | N/A | Java/Kotlin 변경 없음 |

본 태스크의 변경 파일은 모두 마크다운 문서(`.md`)이며, 프로그램 언어 lint/format 검사는 적용 불가. 마크다운 자체의 구조적 검증은 위 Gap Analysis 표(설계-구현 1:1 매칭) 및 smoke check S1~S7로 대체했다.

## Code Quality Findings

본 산출물은 마크다운 명세 문서이므로 전통적인 코드 품질(보안 취약점, 에러 핸들링 누락, 복잡도 등)은 평가 대상이 아니다. 명세 문서 관점의 품질 항목으로 재해석해 검토했다.

### Critical
- 없음.

### Warnings
- 없음.

### Info
- **(Info-1) 슬러그 중복 시 정책이 두 가지로 표현**: Step 1의 2번 항목과 Error Handling 표 모두 "`<slug>-2/-3` 자동 부여 **또는** overwrite 여부 confirm" 두 옵션을 OR로 기술한다. 어느 쪽이 default인지 명세가 한쪽으로 못박혀 있지 않다. 후속 impl/사용 단계에서 자동 suffix를 default로 정한다는 추가 결정이 있으면 명세를 한 줄 다듬을 여지가 있다. (현 시점에서는 LLM 인터프리터가 어느 쪽이든 합리적으로 처리 가능하므로 차단 사유 아님.)
- **(Info-2) Step 3의 `--from` 모드에서 "전부 생략 가능"**: L110에 "4건 중 일부만, 또는 전부 생략 가능"으로 적혀 있다. 전부 생략 시 Step 3을 호출조차 하지 않는 흐름이 가능한데, 이 경우 Step 4 문서의 "Stakeholders/Goals/Constraints/NFR" 섹션 채움 책임이 100% --from 베이스 본문에 위임된다. 의도된 흐름이지만, 향후 사용 사례에서 베이스 문서 품질이 낮으면 빈 섹션이 생길 가능성이 있다는 점만 기록.
- **(Info-3) 다른 jira-task-* 스킬과 일관된 패턴**: Frontmatter 형식, Language Rule 문구, Workflow 단계별 Heading 레벨, Completion Summary 포맷이 기존 스킬(plan/design/impl/test/review/done)과 동일하여 인덱싱·자동완성·향후 라우팅(MAE-119) 측면에서 일관성이 우수.

## Positive Notes

- **설계 추적성**: design 문서의 Implementation Plan 14개 항목이 SKILL.md의 동일 순서·동일 의도로 1:1 반영. 추적성 100%.
- **Non-goals 명시의 강함**: "Jira 이슈 안 만듦", "`.jira-context.json` 안 건드림", "`completedSteps` 갱신 안 함" 등 향후 의존 태스크(MAE-116/117/118/119)와의 책임 경계가 칼같이 그어져 있다. 이는 sub-task 분리의 정수.
- **인라인 fallback 전략**: `templates/requirements.template.md`(MAE-117)이 부재해도 본 스킬이 단독 실행 가능하도록 fallback 전문(全文)이 SKILL.md 안에 박혀 있어, 의존성 부재가 본 태스크 완료를 막지 않는다. 의존 태스크 순서 무관하게 검증 가능.
- **Error Handling의 망라성**: 설계가 11개를 요구했는데 13개로 더 풍부하게 채워졌다(슬러그 위험 문자, 1MB 경고 추가). Out of design scope가 아닌 합리적 보강.
- **Test Plan smoke check 100% 통과**: design 문서가 정의한 S1~S7 7건 모두 단순 grep으로 검증되며, 회귀 검사 자동화도 용이.

---

Reviewed by Claude Code
