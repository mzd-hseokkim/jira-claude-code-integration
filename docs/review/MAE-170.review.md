# Code Review: MAE-170

**Date**: 2026-04-27
**Branch**: feature/MAE-170
**Base**: main
**Reviewer**: Claude Code

## Summary

**결과**: **Approve**

설계 문서의 Implementation Plan 5개 항목이 모두 구현되었고(매칭률 100%), Critical/Warning 이슈 없음. SKILL.md(LLM 가이드) + plugin.json 변경에 한정된 surgical change. Plan에서 식별한 주요 위험(false positive, prose 휴리스틱 모호성)에 대한 완화책이 SKILL.md 본문에 명시적으로 박혀 있다.

## Identified Changes

### Modified Files (2)

| 파일 | 변경 |
|------|------|
| `skills/jira-task-discover/SKILL.md` | +78줄 (Step 3.5 신규 + Step 4 Open Questions 1줄 갱신 + Step 4.5 L204 갱신 + Inline Fallback Template `[CONFLICT]` 예시 1줄 추가) |
| `.claude-plugin/plugin.json` | version `0.17.0` → `0.17.1` |

### New Files (Untracked)

| 파일 | 용도 |
|------|------|
| `docs/plan/MAE-170.plan.md` | 기획 문서 |
| `docs/design/MAE-170.design.md` | 설계 문서 |
| `docs/test/MAE-170.test-report.md` | 테스트 리포트 |

### Total

- 수정: 2 파일 (코드/설정)
- 신규: 3 파일 (문서)
- diff stat: `+78 -4` (코드/설정 부분만)

## Gap Analysis

**설계-구현 일치율: 100% (5/5)**

| # | Design Implementation Plan 항목 | 구현 여부 | 파일 위치 | 비고 |
|---|-------------------------------|----------|----------|------|
| 1 | Step 3 종료(L121) ↔ Step 4 진입 사이에 신규 "Step 3.5: Conflict Detection (--from mode only)" 섹션 삽입 (진입 조건 + 4 카테고리 비교 규칙 + prose 휴리스틱 + 격상 형식 + Step 4/4.5 협력) | O | `skills/jira-task-discover/SKILL.md:123-194` | Decision 절의 6개 소제목(진입 조건/비교 대상 4 카테고리/Prose 휴리스틱/격상 형식/Trace marker 상호 배타/`--lite + --from` 정합성/Step 4 협력/Step 4.5 협력) 모두 명시 |
| 2 | Step 4 Open Questions 합성 가이드(L141 부근)에 "Step 3.5에서 격상된 `[CONFLICT]` 항목 자동 포함" 1줄 추가 | O | `skills/jira-task-discover/SKILL.md:214` | TBD 항목과 함께 나열 + 순서 명시 |
| 3 | Step 4.5 L204의 "예약 문구"를 실제 동작 명세로 갱신 | O | `skills/jira-task-discover/SKILL.md:277` | 1건 이상 시 합성 요약보다 먼저 표시, 4건 이상 시 상위 3개 + 외 N건 축약, conflict 0건 시 미표시 |
| 4 | Inline Fallback Template Open Questions placeholder(L375-379)에 `[CONFLICT]` 예시 1개 추가 | O | `skills/jira-task-discover/SKILL.md:448-453` | placeholder 가이드 문구 갱신 + 예시 1개 추가 |
| 5 | `.claude-plugin/plugin.json` version 증가 (`0.17.0` → `0.17.1`) | O | `.claude-plugin/plugin.json:3` | semver patch +1, 형식 유효 |

**Acceptance Criteria 충족 (AC-1 ~ AC-11)**: 11개 AC 전부 충족 (test-report.md 참조).

## Lint & Format

| 도구 | 대상 파일 수 | 결과 | 비고 |
|------|------------|------|------|
| ESLint / Prettier | 0개 | N/A | 변경 파일이 `.md`/`.json` (.js/.ts/.jsx/.tsx 미해당) |
| ruff / flake8 | 0개 | N/A | 변경 파일이 `.md`/`.json` (.py 미해당) |
| Java/Kotlin checkstyle | 0개 | N/A | 코드 미해당 |
| **JSON 문법** | 1개 | **Pass** | `node -e "JSON.parse(...)"` 통과 |

본 프로젝트는 Node.js/Python/Java/Kotlin 코드 프로젝트가 아닌 Claude Code 플러그인 (스킬 SKILL.md + plugin.json 중심). lint 도구 적용 대상 없음.

## Code Quality Findings

### Critical

없음.

### Warning

없음.

### Info

#### Info-1: 멀티 conflict 예시 미포함 (선택 개선)
- **위치**: `skills/jira-task-discover/SKILL.md:177`
- **현황**: "한 카테고리에 conflict가 여러 개면 각각 별도 항목으로 격상한다 (멀티 conflict 허용, 같은 카테고리 묶기 없음)" 본문은 있으나 별도 예시 없음
- **제안**: 향후 LLM 추론 일관성을 더 보장하려면 같은 카테고리 멀티 conflict 예시 1건을 격상 형식 표준 영역에 추가할 수 있음
- **결정**: 본 이슈 description의 AC에 명시되지 않았고 본문 설명만으로도 LLM이 추론 가능하므로 **현재 상태로 OK**. 향후 LLM 추론 결과에서 일관성 깨짐이 관찰되면 후속 개선

#### Info-2: lint/CI 자동화 부재
- **위치**: 본 이슈 범위 외
- **현황**: 격상 형식 표준 위반(괄호 누락, prefix 누락 등)을 검출할 자동 검증 없음. 본 이슈는 SKILL.md 가이드만 박는 인프라 변경
- **결정**: design 문서 Out of Scope에 명시됨. 후속 lint/CI 이슈로 추적 가능

## Security Review

| 점검 항목 | 결과 | 비고 |
|----------|------|------|
| 보안 취약점 (injection/XSS) | N/A | 코드 변경 없음 |
| 하드코딩된 credentials/tokens | N/A | 코드 변경 없음 |
| 민감 정보 인용 가이드 | **Pass** | SKILL.md L168에 "민감 정보 redact" 가이드 명시 (`<원본>`/`<답변>` 인용에 자격증명·토큰·PII 포함 시 `***` 또는 한 줄 요약으로 대체) |
| 외부 API/네트워크 호출 추가 | None | LLM 추론 외 외부 호출 추가 없음 |
| `--from` 파일 신뢰 경계 | Unchanged | 기존 SKILL.md L60-68 처리 그대로 (본 이슈로 새 위협 추가 없음) |

## Positive Notes

1. **설계-구현 100% 일치**: Design 문서 Implementation Plan 5개 행이 모두 surgical하게 구현됨. 추가 변경 없음 (No scope creep)
2. **Plan에서 식별한 주요 위험에 대한 명시적 완화**:
   - High 위험 (false positive 폭주): "단순 추가 정보(non-contradictory addition)는 conflict 아님" 가이드를 prose 휴리스틱에 강조 (L153)
   - Medium 위험 (prose 휴리스틱 모호성): 4 카테고리별 conflict 인정 케이스를 표로 명시 (L141-146)
   - High 위험 (LLM 형식 일관성 깨짐): 격상 형식 표준 코드 블록 + 예시 2건 + Inline Fallback Template placeholder에 [CONFLICT] 예시 명시 (LLM 모방 학습용)
3. **상호 운용성 명시**: MAE-169 trace marker와의 상호 배타성을 명시적으로 다뤄, 한 항목에 두 종류 표기가 동시 부착되는 hallucination 위험 사전 차단 (L179-182)
4. **단일 책임 분리**: Step 3(질문/답변), Step 3.5(detection), Step 4(합성), Step 4.5(confirm)의 책임 경계가 명확. 각 Step에서 다른 Step의 책임을 침범하지 않음
5. **Surgical change**: 변경 파일이 SKILL.md + plugin.json 2개로 한정. 다른 jira-task-* 스킬, commands, templates 미변경
6. **CLAUDE.md 규칙 준수**: plugin.json version을 정확히 patch +1. 한국어 출력 규칙, Jira 코멘트 영어 헤더 + 한국어 본문 규칙, PDCA 문서 위치 규칙 모두 준수
7. **테스트 커버리지**: 14개 TC 모두 PASS, AC 11개 모두 충족
8. **MAE-168 협력 슬롯 충실 활용**: Step 4.5 L204에 미리 예약된 인터페이스 슬롯을 본 이슈에서 정확히 채움 (예약 문구 → 실제 동작 명세 대체)

## Open Items / Follow-ups

- 후속 이슈 후보 (본 이슈 범위 외):
  - lint/CI 자동화로 격상 형식 표준 위반 검출
  - MAE-171(`requirements.template.md` placeholder 강화)에서 본 이슈의 `[CONFLICT]` 형식을 외부 템플릿 파일에 동기화 (MAE-171이 MAE-169를 blocked-by로 이미 명시)

## Verdict

**Approve** — 설계 일치율 100%, Critical/Warning 이슈 없음, surgical change, 모든 AC 충족. 머지 가능.

다음 단계: `/jira-local-merge MAE-170` → `/jira-task pr MAE-170` → `/jira-task done MAE-170`
