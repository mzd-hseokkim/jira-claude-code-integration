# Code Review: MAE-117

**Date**: 2026-04-26
**Branch**: feature/MAE-117
**Base**: main
**Task**: [1.1.4] templates/requirements.template.md 생성

## Summary

| Metric | Value |
|--------|-------|
| 결과 | **Approve** |
| 검토 파일 수 | 1개 (코드/템플릿) + 3개 (PDCA 문서) |
| 커밋 수 | 0개 (현재 untracked 상태로 보존, 병합 단계에서 커밋 예정) |
| 설계-구현 매칭률 | **100%** |
| Lint/Format | N/A (코드 파일 변경 없음) |
| Critical 이슈 | 0 |
| Warning | 0 |
| Info | 1 |

전반적으로 plan과 design에서 합의된 "단일 마크다운 템플릿 파일 작성" 스코프를 정확히 이행했고, fallback과 바이트 단위 동일성을 추가로 입증했다. 코드 변경이 없는 정적 자산이므로 보안/성능 리스크가 없으며, 모든 AC가 충족되어 **Approve**로 판정한다.

## Changed Files

| 파일 | 종류 | 비고 |
|------|------|------|
| `templates/requirements.template.md` | 신규 (62 lines) | 본 태스크 핵심 산출물 |
| `docs/plan/MAE-117.plan.md` | 신규 PDCA | 계획 문서 |
| `docs/design/MAE-117.design.md` | 신규 PDCA | 설계 문서 |
| `docs/test/MAE-117.test-report.md` | 신규 PDCA | 테스트 리포트 |

> Note: 4개 파일 모두 현재 untracked 상태이며, `/jira-local-merge` 단계에서 일괄 커밋된다. 본 리뷰는 작업 트리 상태를 기준으로 수행했다.

## Gap Analysis

Design 문서 `docs/design/MAE-117.design.md`의 Implementation Plan(L92-L106) 항목을 체크리스트로 변환하여 실제 구현과 비교.

| Design 항목 | 구현 여부 | 위치/증거 | 비고 |
|------------|----------|----------|------|
| 신규 파일 `templates/requirements.template.md` 생성 | O | 파일 존재, 62 lines, 1307 bytes | AC-1 충족 |
| 메타 헤더 4개 (Slug/Mode/Generated At/Source) bullet list | O | L3-L6 | fallback과 1:1 동일 |
| H2 섹션 10개 순서 일치 | O | L8, L12, L16, L20, L24, L28, L35, L42, L49, L55 | AC-2 충족 |
| 제목 `# Requirements: <Topic>` | O | L1 | fallback 동일 |
| `<...>` placeholder 표기 채택 (`{...}` 0건) | O | grep 결과 0건 | AC-5 충족 |
| `Edge Cases`, `Out of Scope` 위 HTML 주석 안내 | O | L37, L44 (`<-- --lite 모드면 ... -->`) | AC-4 충족 |
| `Functional Requirements` 번호 매김 예시 2줄 | O | L32-L33 (`1. <Req-1>`, `2. <Req-2>`) | Design 작성 규칙 충족 |
| `Proposed Issue Breakdown` Epic/Story/Sub-task 트리 | O | L57-L62 (Epic 1 / Story 2 / Sub-task 3개) | Design 작성 규칙 충족 |
| 파일 끝 LF 1개 (POSIX) | O | `xxd` 확인 (`...3e 0a`) | Design 작성 규칙 충족 |

**매칭률**: 9/9 = **100%**

추가 검증(설계 명세를 초과하는 항목): test 단계에서 fallback 본문(`SKILL.md` L185-L246)과 신규 파일을 `diff`로 비교해 **IDENTICAL**(차이 0줄)임을 확인. AC-2/AC-4/AC-5의 핵심 의도(fallback과의 호환)를 가장 강하게 보장한다.

## Lint & Format

| 도구 | 대상 파일 수 | 결과 |
|------|------------|------|
| ESLint / Prettier | 0개 | N/A (Node.js 프로젝트 아님, 변경된 JS/TS 없음) |
| ruff / flake8 | 0개 | N/A (Python 코드 변경 없음) |
| checkstyle | 0개 | N/A (Java/Kotlin 코드 변경 없음) |

본 태스크 변경 파일은 모두 `.md`로, Step 2.5 lint/format 도구 매트릭스 적용 대상이 아니다. 마크다운 자체 품질(섹션 구조/표기 일관성/개행)은 Code Quality Review에서 다룬다.

## Code Quality Findings

### Critical

없음.

### Warnings

없음.

### Info

- **(Info-1) `Edge Cases`/`Out of Scope` 안내 주석의 위치 의도**: 현재 안내 주석(`<-- --lite 모드면 ... -->`)이 H2 헤딩 아래에 배치되어 있다. fallback과 1:1 동일하므로 호환성 측면에서는 문제 없으나, 향후 MAE-106(Phase 1.3 정비) 시 "헤딩 위/아래 일관성"을 표준화할 때 함께 검토할 가치가 있다. 본 태스크 범위 외이며, 변경 권고는 아니다 (`docs/design/MAE-117.design.md` Implementation Plan L101도 동일 위치 의도를 명시).

## Positive Notes

- **Fallback과의 바이트 단위 동일성 보장**: `diff` IDENTICAL을 직접 검증해 AC-2/AC-4/AC-5의 호환 의도를 정합 수준에서 입증했다. 단순 시각 검토를 넘어선 강한 보증이다.
- **YAGNI 충실**: discover 스킬 코드를 건드리지 않고 "파일 존재 여부 분기"라는 기존 메커니즘에만 의존해 임시 봉합을 풀었다. 단일 Write 호출로 끝낼 수 있는 작업을 정확히 그 범위 안에서 끝냈다.
- **PDCA 문서 정합**: plan에서 합의된 "경량 design"을 design 문서 NotesL207-L209가 명시적으로 언급하고, 그 흐름이 그대로 impl/test에 반영되었다. 의사결정 근거가 추적 가능하다.
- **Out of Scope 명시**: discover 스킬 변경, 다른 템플릿 파일, CI lint, INDEX 자동 관리 등을 plan/design 모두에서 후속 태스크로 분리해 스코프 크리프를 차단했다.
- **검증 방식의 명령 단위 명세화**: Test Plan TC-1~TC-5 모두 `grep`/`wc`/`ls`/`diff` 등 재현 가능한 명령을 그대로 명시해, review 단계에서 동일하게 재검증할 수 있게 했다.

## Verification Commands (재현용)

```bash
# 섹션 구조
grep -n '^## ' templates/requirements.template.md
# 메타 헤더
grep -E '^- \*\*(Slug|Mode|Generated At|Source)\*\*' templates/requirements.template.md
# --lite 안내
grep -n -E '<-- --lite' templates/requirements.template.md
# 표기 규약 (`{...}` 등장 0건 확인)
grep -nE '\{[^}]+\}' templates/requirements.template.md ; echo "exit=$?"
# Fallback 정합 (IDENTICAL 기대)
awk 'NR>=185 && NR<=246' skills/jira-task-discover/SKILL.md > /tmp/fallback.md
diff /tmp/fallback.md templates/requirements.template.md
```

---

Reviewed by Claude Code
